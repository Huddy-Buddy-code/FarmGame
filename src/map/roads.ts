/**
 * OSM attribution (brief §2, §13).
 *
 * OSM data is ODbL — attribution is required wherever OSM-derived content is shown.
 * Road *rendering* lives in `osmRoadsVector.ts` (real geometry as vector lines).
 */

export const OSM_ATTRIBUTION =
  "© OpenStreetMap contributors (ODbL)";
